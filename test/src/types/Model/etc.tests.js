import {
  expect,
  itMaps,
  _,
  nrser,
  t,
} from '../../testHelpers';

const Model = nrser.t.Model;

describe("types/Model.js", () => {
  describe("Model", () => {
    describe("[assorted crap / playground]", () => {
      
    }); // [assorted crap / playground]
  }); // Model
}); // types/Model.js