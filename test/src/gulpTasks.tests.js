import chai, { expect } from 'chai';
import { itMaps2 } from '../../lib/testing.js';
import { GulpTasks } from '../../lib/gulpTasks.js';

describe('gulpTasks.js', () => {
  describe('GulpTasks', () => {
    it("constructs", () => {
      expect(true).to.be.false;
    });
  });
});